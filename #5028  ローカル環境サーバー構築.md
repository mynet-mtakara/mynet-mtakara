```
@startuml
map "プラットフォーム偽装サーバー" as pf {
IP=>dhcp相当
s/w => apache or nginx
}

map php {
IP=>172.28.0.2
s/w => apache,php
}
map db {
IP=>172.28.0.3
}
map cacher {
IP=>172.28.0.3
s/w => redis
}
map cachem {
IP=>172.28.0.3
s/w => memchashed
}
map "ユーザー" as user {
}
user --> pf
pf --> php
php -right-> cacher
php -left-> db
php -down-> cachem
note bottom of db : テスト用とローカル用に分割
@enduml
```
